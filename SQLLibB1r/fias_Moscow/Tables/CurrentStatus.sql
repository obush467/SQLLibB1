CREATE TABLE [fias_Moscow].[CurrentStatus] (
    [CURENTSTID] INT            NOT NULL,
    [NAME]       NVARCHAR (100) NOT NULL,
    CONSTRAINT [PK_CurrentStatus] PRIMARY KEY CLUSTERED ([CURENTSTID] ASC)
);

