CREATE TABLE [fias_Moscow].[HouseStateStatus] (
    [HOUSESTID] INT          NOT NULL,
    [NAME]      VARCHAR (60) NOT NULL,
    CONSTRAINT [PK_HouseStateStatus] PRIMARY KEY CLUSTERED ([HOUSESTID] ASC)
);

