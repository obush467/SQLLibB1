CREATE TABLE [fias_Moscow].[IntervalStatus] (
    [INTVSTATID] INT          NOT NULL,
    [NAME]       VARCHAR (60) NOT NULL,
    CONSTRAINT [PK_IntervalStatus] PRIMARY KEY CLUSTERED ([INTVSTATID] ASC)
);

